### ✨ Features
- **Project Updater**: Added `dsa-lab update` command. Existing users can now run `npx create-dsa-lab@latest update` inside their project to cleanly replace `scripts/` and `templates/` with the latest versions and merge in missing dependencies, without affecting their `src/` files or config. (Closes #2)
